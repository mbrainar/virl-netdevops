<devices xmlns="http://tail-f.com/ns/ncs">
  <authgroups>
    <group>
      <name>{{ lab_name }}</name>
      <default-map>
        <remote-name>cisco</remote-name>
        <remote-password>cisco</remote-password>
      </default-map>
      <umap>
        <local-user>admin</local-user>
        <remote-name>cisco</remote-name>
        <remote-password>cisco</remote-password>
      </umap>
    </group>
  </authgroups>
  <device-group>
    <name>{{ lab_name }}</name>
    {% for node in nodes %}
    <device-name>{{ node.node_label }}</device-name>
    {% endfor %}
  </device-group>
  {% for node in nodes %}
  <device>
    <name>{{ node.node_label }}</name>
    <address>{{ node.address[0] }}</address>
    <ssh>
      <host-key-verification>none</host-key-verification>
    </ssh>
    <authgroup>{{ lab_name }}</authgroup>
    <device-type>
      <cli>
        <ned-id xmlns:{{ node.ned.prefix }}="{{ node.ned.ns }}">{{ node.ned.prefix }}:{{ node.ned.ned }}</ned-id>
        <protocol>ssh</protocol>
      </cli>
    </device-type>
    <state>
      <admin-state>unlocked</admin-state>
    </state>
  </device>
  {% endfor %}
</devices>
